extensions:
  myextension:
  
receivers:
  myreceiver:

processors:
  myprocessor:

exporters:
  myexporter:

service:
  extensions: [myextension]
  pipelines:
    traces:
      receivers: [myreceiver]
      processors: [myprocessor]
      exporters: [myexporter]