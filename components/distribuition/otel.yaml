extensions:
  myextension:

receivers:
  myreceiver:
  otlp:
    protocols:
      grpc:
      http:

processors:
  myprocessor:

exporters:
  debug:
    verbosity: detailed

service:
  extensions: [myextension]
  pipelines:
    traces:
      receivers: [myreceiver]
      processors: [myprocessor]
      exporters: [debug]