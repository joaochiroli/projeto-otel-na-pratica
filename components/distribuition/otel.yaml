extensions:
  myextension:

receivers:
  myreceiver:
  otlp:
    protocols:
      grpc:
      http:

exporters:
  debug:
    verbosity: detailed

service:
  extensions: [myextension]
  pipelines:
    traces:
      receivers: [myreceiver]
      exporters: [debug]